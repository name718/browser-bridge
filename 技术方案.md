# Browser Bridge 技术方案

## 1. 背景

本地 AI Agent，例如 Claude Code、Codex、Gemini CLI，通常无法直接操作用户浏览器，也无法访问依赖浏览器登录态、Cookie、企业权限或内网环境的网站。

Browser Bridge 的目标是通过 Chrome 插件和本地 MCP Server 建立桥接：

- Chrome 插件运行在用户真实浏览器环境中，负责读取页面内容和执行页面操作。
- MCP Server 运行在本地，负责向 AI Agent 暴露标准化 MCP tools。
- AI Agent 通过 MCP tools 间接读取和操作浏览器页面。

该方案不会绕过网站权限，只使用用户浏览器当前已经拥有的访问权限。

## 2. 目标

### 2.1 核心目标

- 支持本地 AI Agent 读取当前 Chrome 页面内容。
- 支持读取已登录、有权限、内网站点的页面信息。
- 支持基础浏览器操作，例如打开页面、点击、输入、滚动、等待元素。
- 通过 MCP 标准协议对外提供能力。
- 提供安全、可控、可审计的操作边界。

### 2.2 非目标

第一版不做以下能力：

- 不提供任意 JavaScript 执行能力。
- 不读取或导出 Cookie。
- 不做网络请求拦截。
- 不绕过网站登录、权限、验证码或风控。
- 不默认支持支付、删除、发布等高风险自动操作。

## 3. 总体架构

```text
AI Agent
  |
  | MCP tools
  v
Local MCP Server
  |
  | WebSocket / localhost
  v
Chrome Extension Background Service Worker
  |
  | chrome.tabs / chrome.scripting / runtime messaging
  v
Content Script
  |
  v
Target Web Page
```

## 4. 核心模块

### 4.1 MCP Server

MCP Server 是本地桥接服务，负责对 AI Agent 暴露浏览器能力。

职责：

- 启动 MCP 服务。
- 注册浏览器相关 MCP tools。
- 管理 Chrome 插件连接状态。
- 将 MCP tool call 转换为插件指令。
- 管理请求 ID、超时、错误码。
- 输出本地操作日志。
- 执行基础安全校验，例如 allowlist、敏感操作拦截。

建议技术栈：

- Node.js
- TypeScript
- 官方 MCP TypeScript SDK
- `ws` WebSocket 库
- Vitest 单元测试

### 4.2 Chrome Extension

Chrome 插件负责持有浏览器权限，并与 MCP Server 建立本地连接。

职责：

- 通过 Background Service Worker 连接本地 MCP Server。
- 管理 tab、window 和 content script 注入。
- 接收 MCP Server 指令。
- 将指令转发到目标页面 content script。
- 返回页面读取结果或操作结果。
- 提供 popup 页面展示连接状态和安全设置。

建议技术：

- Chrome Extension Manifest V3
- TypeScript
- `chrome.tabs`
- `chrome.scripting`
- `chrome.runtime`
- `activeTab`
- 可选 `host_permissions`

### 4.3 Content Script

Content Script 运行在目标页面中，负责实际读取 DOM 和执行页面操作。

职责：

- 提取页面标题、URL、可见文本。
- 提取链接、按钮、输入框、表格、表单控件。
- 生成可操作元素列表。
- 为元素生成 `elementId`。
- 执行点击、输入、清空、选择、滚动等操作。
- 返回结构化结果。

### 4.4 Shared Protocol

MCP Server 和 Chrome Extension 之间使用统一的 JSON 消息协议。

请求格式：

```ts
type BridgeRequest = {
  id: string;
  tool: string;
  params?: Record<string, unknown>;
  tabId?: number;
  timeoutMs?: number;
};
```

响应格式：

```ts
type BridgeResponse = {
  id: string;
  ok: boolean;
  data?: unknown;
  error?: {
    code: string;
    message: string;
    details?: unknown;
  };
};
```

## 5. MCP Tools 设计

### 5.1 MVP Tools

第一版建议提供：

```text
browser_status
browser_get_active_tab
browser_get_page_text
browser_get_page_snapshot
browser_click
```

### 5.2 基础操作 Tools

```text
browser_list_tabs
browser_open_url
browser_activate_tab
browser_type
browser_clear
browser_select
browser_scroll
browser_wait_for
```

### 5.3 可视能力 Tools

```text
browser_screenshot
browser_element_screenshot
```

### 5.4 后续增强 Tools

```text
browser_get_table
browser_get_forms
browser_get_frames
browser_get_downloads
browser_get_canvas_screenshot
browser_debug_info
```

### 5.5 暂不开放的高风险 Tools

```text
browser_eval_js
browser_get_cookies
browser_set_cookies
browser_network_intercept
```

这些能力风险较高，后续如需要应放入 Advanced Mode，并要求用户显式开启。

## 6. 页面快照数据结构

```ts
type PageSnapshot = {
  tabId: number;
  url: string;
  title: string;
  text: string;
  elements: BrowserElement[];
};

type BrowserElement = {
  elementId: string;
  role: string;
  tagName: string;
  text?: string;
  ariaLabel?: string;
  placeholder?: string;
  value?: string;
  href?: string;
  visible: boolean;
  disabled: boolean;
  selectorHint?: string;
  rect?: {
    x: number;
    y: number;
    width: number;
    height: number;
  };
};
```

## 7. 元素定位策略

页面操作优先使用稳定的 `elementId`。

定位优先级：

1. `elementId`
2. CSS selector
3. 可见文本
4. 坐标点击

MVP 阶段建议只支持 `elementId`、CSS selector 和文本匹配。坐标点击放到截图能力成熟后再引入。

## 8. 安全设计

### 8.1 本地连接安全

- MCP Server 只监听 `127.0.0.1`。
- 插件主动连接本地 MCP Server。
- 首次连接使用 pairing token。
- 未配对连接直接拒绝。

### 8.2 域名权限控制

- 支持 allowlist。
- 支持 denylist。
- 默认可配置为仅允许当前 tab 读取。
- 高风险域名可强制只读。

### 8.3 敏感信息保护

默认脱敏：

- password input value
- token
- secret
- authorization
- cookie
- session
- credential

Content Script 不应读取 password 输入框的真实值。

### 8.4 高风险操作确认

以下操作建议需要用户确认：

- 删除
- 支付
- 发布
- 提交审批
- 发送消息
- 提交表单
- 修改权限

确认可以由插件 popup 或浏览器内 overlay 完成。

### 8.5 审计日志

建议记录：

- 时间
- tool 名称
- tab URL
- 操作类型
- 操作目标
- 成功或失败
- 错误原因

日志不记录完整敏感内容。

## 9. 错误码设计

建议使用稳定错误码：

```text
BROWSER_NOT_CONNECTED
TAB_NOT_FOUND
TAB_NOT_ACTIVE
PERMISSION_DENIED
DOMAIN_NOT_ALLOWED
CONTENT_SCRIPT_NOT_READY
ELEMENT_NOT_FOUND
ELEMENT_NOT_VISIBLE
ELEMENT_DISABLED
ACTION_TIMEOUT
USER_CONFIRMATION_REQUIRED
USER_REJECTED
UNSUPPORTED_PAGE
INTERNAL_ERROR
```

## 10. 项目结构建议

```text
browser-bridge/
  packages/
    mcp-server/
      src/
        index.ts
        tools/
        bridge/
        security/
        logger/
    extension/
      src/
        background/
        content/
        popup/
        shared/
      manifest.json
    shared/
      src/
        protocol.ts
        types.ts
  docs/
    architecture.md
    security.md
    mcp-tools.md
  package.json
  pnpm-workspace.yaml
```

## 11. 技术风险

### 11.1 插件 Service Worker 休眠

Manifest V3 background 是 service worker，可能休眠。

应对：

- 使用心跳机制。
- 支持自动重连。
- MCP Server 对离线状态返回明确错误。

### 11.2 复杂页面兼容性

问题：

- iframe
- Shadow DOM
- Canvas
- WebGL
- 虚拟列表
- 动态渲染页面

应对：

- MVP 先支持普通 DOM。
- 后续逐步支持 iframe、Shadow DOM、截图、Canvas。

### 11.3 安全边界

最大风险是 Agent 误操作用户页面。

应对：

- 默认低权限。
- 高风险操作确认。
- 域名 allowlist。
- 禁止默认任意 JS。
- 完整审计日志。

## 12. 推荐路线

第一版应优先证明完整闭环：

1. MCP Server 能启动。
2. Chrome Extension 能连接 MCP Server。
3. Agent 能读取当前页面内容。
4. Agent 能基于元素执行一次点击。
5. 操作过程有明确错误返回和日志。

第一版不追求万能浏览器自动化，优先做安全、稳定、可解释的浏览器桥。
